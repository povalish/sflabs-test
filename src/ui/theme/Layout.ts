import { StyleSheet } from 'react-native';
import { Units } from './Units';



export const Layout = StyleSheet.create({
  flexContainer: {
    flex: 1,
  },
  wrapper: {
    paddingHorizontal: Units.Spacers.x1,
  },
});
